# PLAYBOOK ─ 実装チケット集(モデル引き継ぎ用・作業指示書)

> **このファイルは、今後のAIセッション(Opus/Sonnet等)が1チケットずつ独立で実行できるように書かれた作業指示書。**
> 全体工程は [`ROADMAP.md`](ROADMAP.md)、技術設計は [`ARCHITECTURE.md`](ARCHITECTURE.md) が正本。矛盾したらそちらが勝つ。
> チケットID = `P<フェーズ>-<連番>`。**原則1セッション=1チケット、1チケット=1コミット。**

最終更新: 2026-07-03(Fable 5 作成)

---

## 0. セッション運用プロトコル(全チケット共通・厳守)

### 0.1 セッションの始め方
1. `ROADMAP.md` で現在フェーズと完了済みチェックを確認。
2. 本ファイルの該当チケットを読む。「参照」欄の設計mdの該当節を**必ず読んでから**着手。
3. 着手前に `git status` がクリーンであることを確認(散らかっていたら先に報告)。

### 0.2 作業の掟
- **設計→md反映→実装**の順。仕様に迷ったら勝手に決めず、設計mdを確認→無ければユーザーに質問。
- UIは `DESIGN.md` 準拠(100dvh・角丸四角・色トークン・ドット文字の使い分け)。
- キャラ絵は `tools/PXART_PROCESS.md` 完全準拠。**生成AI(fal.ai等)は使わない。鼻と口は絶対に描かない。**
- 画面間の本番往復プロトコル(`ARCHITECTURE §2`)を壊さない。localStorageスキーマ変更は必ず `migrate()` 追加+`ARCHITECTURE §3` 追記とセット。
- 得点計算の配置原則(`ARCHITECTURE §5`): 舞台の上=manzai-play、舞台の外=success-play。同じ式を両側に書かない。
- 一時ファイルは `_` 接頭辞(gitignore済)。使い捨て確認HTMLは役目を終えたら `_archive/` へ。

### 0.3 技術的な注意(ハマりどころ)
- HTML/JSはUTF-8。**PowerShellの`Get-Content`で日本語が化ける**ので、ファイル内容はReadツールで読むこと。
- 巨大HTML(success-play 55KB等)は全読みせず、Grepで関数名を当ててから該当行をReadする。
- Windows PowerShell 5.1(`&&`不可・ternary不可)。POSIXが要るならBashツール。
- mp3をfetchするため `file://` 直開き不可。検証は静的サーバ経由(下記0.4)。
- iOS Safariのオーディオは初回タップ解禁が必要 → 既存の `ensureAudio()` パターンを踏襲。

### 0.4 検証と完了処理(全チケット共通のDoD付帯条件)
1. ローカル静的サーバ(ポート8090慣例)で対象画面を開き、コンソールエラーゼロを確認。
2. 実機確認が要るもの(UI/演出/操作)は cloudflared トンネルURLをユーザーに案内して確認を得る。
3. 既存フローの回帰確認: 「新規開始→コマンド1回→劇場出番→漫才プレイ→帰還反映」が壊れていないこと。
4. コミット(日本語・1行要約+本文)。`ROADMAP.md` の該当チェックボックスを更新して同コミットに含める。

### 0.5 現行コードの地図(2026-07-03時点)
| ファイル | 中身 | 入口になる主要関数 |
|---|---|---|
| `success-play.html` | 育成ハブ。状態`S`、セーブ、コマンド、劇場/M-1、エンディング | `newGame` `doCommand` `advanceTurn` `applyTheaterResult` `applyAwardResult` `honbanPerf` `openEnding` `maybeApplyHonbanResult` `render` |
| `manzai-play.html` | リズムゲーム。譜面生成、判定、本番モード往復 | 譜面生成(`pm_lineup`読込部, 328行付近)、`HONBAN`分岐(335行付近) |
| `neta-builder.html` | 9枠打線編成 | `pm_parts`読込/`pm_lineup`保存 |
| `index.html` | タイトル・3画面への導線 | ─ |

---

# Phase 1. コアループ完成(α版)

> 目標:「名前入力→OP→イベントの混ざった3年→エンディング」の通しプレイ。
> 依存順: P1-01 → P1-02 → P1-03 → P1-04 → (P1-05〜P1-09は並行可)

## P1-01 セーブスキーマv2とマイグレーション基盤
- **目的**: 以後のチケットが安全にフィールドを足せる土台。
- **参照**: `ARCHITECTURE §3.3`
- **触る**: `success-play.html`
- **手順**:
  1. `S` 初期化(`newGame`)に `v:2, year(=yearOf(turn)キャッシュ不要なら省略), tokusyu:[]` を追加。
  2. `loadGame()` 冒頭に `migrate(save)`: `v`無し→v2へ(欠損フィールドにデフォルト付与)。以後 `v===SAVE_V` まで順送りする switch 構造にする。
  3. 新キー `pm_flags`(`{seen:{},vars:{}}`)と `pm_meta`(`{endings:{},combos:{},runs:0}`)のload/save関数を追加。**`clearSave()` は `pm_meta` を消さない**。`newGame()` は `pm_flags` をリセットし `pm_meta.runs++`。
- **受け入れ**: 旧セーブ(手でv抜きJSONを入れて)を読んでも壊れない。新規開始→リロードで継続できる。`clearSave`後も`pm_meta`が残る。
- **検証**: DevToolsのApplication→localStorageで3キーの中身を目視。

## P1-02 シナリオエンジン(紙芝居再生機)
- **目的**: SCRIPTS/EVENTSの台本をデータ駆動で再生する共通機構。**Phase 1の最重要チケット。**
- **参照**: `ARCHITECTURE §4.1`(データ形式・step語彙は本節が正本)
- **触る**: 新規 `js/scenario.js`、新規 `js/data/scripts.js`、`success-play.html`(scriptタグ2本追加+オーバーレイDOM)
- **手順**:
  1. `js/` を新設。`scenario.js` に `playScript(id, ctx, onEnd)` を実装。`ctx={S, flags, applyEff(effObj), unlock(key)}` を呼び出し側(success)が渡す=エンジンは`pm_save`を直接触らない。
  2. オーバーレイUI: 全画面暗転(z-index最上位)、背景色替え(`bg`)、立ち絵(`chara`: `images/`のPNGをL/R/C配置、`face`で差替)、台詞窓(下部・話者名タブ・1文字ずつ文字送り・タップで全文表示→次へ)、選択肢ボタン(縦積み)。見た目はDESIGN.mdのパネル/ボタントークン。
  3. step語彙 `bg/chara/hide/say/choice/label/jump/eff/unlock/se/wait` を実装。`{player}` は `pm_flags.vars.playerName` 展開。`se` は既存WebAudio合成の `blip` 系を流用。
  4. `js/data/scripts.js` にテスト台本 `TEST-01`(bg→2人表示→say×3→choice2択→eff→合流→終了)を置き、デバッグ起動手段(URLに `?script=TEST-01` で再生)を付ける。
- **受け入れ**: TEST-01が再生でき、選択肢で分岐し、`eff:{idea:+5}` がステータスに反映され、終了後に通常画面へ戻る。文字送り中タップ=全文表示、表示後タップ=次stepの2段階が効く。
- **検証**: トンネルURLで実機タップ確認(必須)。

## P1-03 名前入力+オープニングS-00
- **目的**: 物語の入口。エンジンの初実戦。
- **参照**: `SCRIPTS.md S-00「約束」`、`00_OVERVIEW §1.2`(名前はプレイヤー入力)
- **触る**: `success-play.html`、`js/data/scripts.js`
- **手順**:
  1. `newGame()` 冒頭に名前入力モーダル(DESIGN.md準拠。既定値「コウタ」等・最大6文字目安・空なら既定値)→ `pm_flags.vars.playerName` へ。
  2. `SCRIPTS.md` の S-00 を steps 形式へ変換(**台詞は一字一句コピー**。要約・改変禁止。長台詞は `say` を分割)。登場人物の立ち絵が無い場合(美咲)は当面シルエット/名前タブのみで代替し、TODOコメントを残す。
  3. 名前入力→S-00再生→`pm_flags.seen["S-00"]=1`→1週目開始。`pm_meta.runs>=1` の周回時は「スキップ」ボタンを出す。
- **受け入れ**: 新規開始で名前→OP→通常画面。リロードしてもOPが再発しない。2周目でスキップ可。
- **検証**: 新規2回(通し/スキップ)をトンネルで。

## P1-04 イベントエンジン(抽選器)
- **目的**: EVENTS.mdを流し込むための抽選機構。
- **参照**: `ARCHITECTURE §4.2`、`EVENTS.md`冒頭「発生区分の仕分け」
- **触る**: 新規 `js/events.js`、新規 `js/data/events.js`、`success-play.html`
- **手順**:
  1. `pickEvent(pool, S, flags)`: `pool`一致→`cond(S)`真→`once`未消化、の候補から重み付き抽選で1件 or null。
  2. フックを3箇所: ①`doCommand`の`asobi`(既存のランダム結果を「イベントが引けたら台本再生、外れたら現行処理」に変更、発生率70%目安) ②`advanceTurn()`末尾の週送り枠(発生率15%目安・本番週は発生させない) ③`applyTheaterResult()`のモーダル閉じ後(出番後枠・発生率25%目安)。
  3. 発生率の定数は先頭に `EVENT_RATES` としてまとめ、調整可能に。
- **受け入れ**: ダミーイベント1件で3フック全てから再生されること。`once:true` が2度出ないこと。
- **検証**: 発生率を一時100%にして3経路を確認→戻す。

## P1-05 EVENTS.md移植 第1弾(重複可の小イベント9種)
- **目的**: 日常の彩り。変換作業の型を作る。
- **参照**: `EVENTS.md` の「バイト先イベント」「日常の小イベント」(B-01〜03, 遊-01, 休-01, 稽-01, 日-01, 日-02, 劇-01)
- **触る**: `js/data/events.js`、`js/data/scripts.js`
- **手順**: 各台本を steps へ変換(台詞コピー厳守)。プール割当= B系:`cmd:baito`(バイトコマンドが無ければ`kasegu`)、遊-01:`cmd:asobi`、休-01:`cmd:rest`、稽-01:`cmd:awase`、日-01/02:`week`、劇-01:`theater`。効果値はEVENTS.md記載通り、記載が無い場合は小さめ(発想力+2〜4等)にしてコメントで「※値は仮」と明記。
- **受け入れ**: 9種すべて該当プールから出る。効果がステータスに反映される。
- **検証**: `?script=` デバッグ起動で9本を目視→抽選経由でも数本確認。

## P1-06 EVENTS.md移植 第2弾(主要イベント10種)
- **目的**: 選択肢・分岐持ちの本命イベント。
- **参照**: `EVENTS.md` 01(先輩のアドバイス※特殊能力はP1-08後に接続)/03(差し入れ)/07(マネージャー面談)/08(スランプ)/09(街頭認知)/10(矢田の儲け話)/12(深夜番組)/13(ネタ帳紛失)/19(SNS炎上)/21(体調を崩す)
- **手順**: P1-05と同型。分岐は `choice`+`label`。確率分岐(儲け話の1:3:6等)は steps に `roll:{A:1,B:3,C:6}` 語彙を追加してよい(**追加したらARCHITECTURE §4.1へ追記**)。01の特殊能力付与は P1-08 完了までは発想力+へ差し替え、TODOコメント。
- **受け入れ**: 10種が仕様通りの分岐・確率で動く。
- **検証**: 儲け話を20回デバッグ実行して分布が概ね1:3:6。

## P1-07 相方システムの効果実装
- **目的**: 信頼度を「意味のある数字」にする。
- **参照**: `01_SUCCESS §5.6`、`00_OVERVIEW 付録B`(1年目は相方マイナスイベント免除)
- **触る**: `success-play.html`(`honbanPerf`)、`js/data/events.js`
- **手順**:
  1. `honbanPerf()` に信頼度係数: trust 70以上で+5%、90以上で+8%、30未満で−5%(値は§5.6に従い、無ければこの仮値+コメント)。
  2. 相方マイナスイベント(遅刻/ケンカ等、EVENTS.mdに該当があればそれ、無ければ台本を新規に書きEVENTS.mdへも追記)を `week` プールに追加。`cond: S=>yearOf(S.turn)>=2 && S.trust<40`。
  3. ステータス画面の信頼度表示に効果の説明を1行追加。
- **受け入れ**: trustを手で変えて本番出来が変わる。1年目にマイナスイベントが出ない。
- **検証**: コンソールで `S.trust=90` 等にして劇場出番を比較。

## P1-08 特殊能力(最小セット)
- **目的**: 青特/金特/赤特の器と最初の9個。
- **参照**: `03_NETA §2.3`
- **触る**: 新規 `js/data/tokusyu.js`、`success-play.html`、`js/data/events.js`
- **手順**:
  1. データ定義 `{id, name, rank:"青"|"金"|"赤", desc, hook, value}`。第1弾は**育成側`honbanPerf`だけで完結する効果**に限定(例: 青:スベリ耐性=低acc時の下振れ緩和/ツカミ上手=序盤補正/安定感=振れ幅減/客あしらい=人気由来上限緩和/リズム感=acc+3。金: 爆笑製造機=高acc時+10%/約束の男=M-1決勝+5。赤: あがり症=M-1で−5/遅刻癖=信頼低下イベント率UP)。※効果名・値は`03_NETA §2.3`に具体があればそちら優先、無ければ上記を仮実装し**md側へ逆反映**する。
  2. `S.tokusyu` 配列(P1-01で追加済み)、ステータス画面に一覧表示(青=青地/金=金地/赤=赤地のチップ)。
  3. EVENTS 01「先輩のアドバイス」1-a を特殊能力付与に接続(所持済みなら発想力+へフォールバック)。
- **受け入れ**: イベントで取得→ステータスに表示→本番数値に効く、が通る。
- **検証**: 全能力を強制付与するデバッグ関数で `honbanPerf` の差を確認。

## P1-09 コンボ: 発見・図鑑・得点修正
- **目的**: ハクスラ収集の快感の核。
- **参照**: `SCORING §6`(得点修正)、`03_NETA §3.5-3.6`(発見条件)
- **触る**: `success-play.html`、`neta-builder.html`(図鑑表示)、新規 `js/data/combos.js`
- **手順**:
  1. レシピ定義 `{id, name, pair:[tagA,tagB], bonus}` を10本(§3.5の例から。無ければタグ総当りから語感の良い組を仮作成し**03_NETAへ逆反映**)。
  2. 本番リザルト処理(`applyTheaterResult`/`applyAwardResult`)で lineup の隣接ペアを走査→未習得レシピと一致したら「コンボ発見!」モーダル(演出は既存モーダル+`floatPop`)→`S.combos`と`pm_meta.combos`へ。
  3. 得点修正: 習得済みコンボが打線に含まれる本番は `honbanPerf` にボーナス(SCORING §6の式)。
  4. 図鑑UI: ステータスモーダルに「コンボ」タブ。未発見は「???」。
- **受け入れ**: 特定並びで本番→発見→次回から得点が上がる→図鑑に載る。周回後も図鑑が残る。
- **検証**: 発見前後の同一打線で得点比較。

---

# Phase 2. 賞レース完成

> 依存順: P2-01 → P2-02 → P2-03 は譜面側で連続。P2-04 → P2-05 → P2-06 はM-1側で連続。P2-07は独立。

## P2-01 会場温度システム復活
- **目的**: 旧v6の看板システムを現行譜面方式へ移植。
- **参照**: `02_MANZAI_PLAY §4.4`(仕様正本)、`archive/prototype/`の旧v6実装(あれば参考)
- **触る**: `manzai-play.html`
- **手順**:
  1. 温度変数(0〜100、開始30)。Perfect/Goodで上昇(ノートサイズが大きいほど+大)、Bad/Missで下降。
  2. 笑い量計算に温度係数(§4.5の式)。
  3. HUDに温度バー追加。**低温=青→中温=橙→高温=赤のグラデ**(00_OVERVIEW §10.1-3をここで消化)。DESIGN.mdの色トークン使用。
  4. 劇場出番は客入り(人気)で開始温度が変わる: `pm_honban` に `startTemp` を追加(育成側で人気から計算。ARCHITECTURE §3.1の表に追記)。
- **受け入れ**: プレイ中バーが動き色が変わる。高温時と低温時で同じPerfectのスコアが変わる。
- **検証**: わざと全Miss/全Perfectの2プレイで比較。

## P2-02 性質タグのゲームプレイ属性
- **目的**: タグを「譜面が変わる属性」に戻す(v6機能の移植)。
- **参照**: `02_MANZAI_PLAY §4.3`(タグ→属性の対応表が正本)
- **触る**: `manzai-play.html`(譜面生成部・判定部)
- **手順**: §4.3の表の属性を現行「1パーツ=1小節」に対応付けて実装。最低限: 裏拍スカし(該当タグ小節は裏拍ノート化※既存`isBack`活用)/大ボラ(ホールド強制+成功時ボーナス)/シュール=温度依存(高温で威力倍・低温で半減)/相性(隣接タグ一致で小ボーナス)。実装できない属性は「未実装」と`02_MANZAI_PLAY`へ注記。
- **受け入れ**: タグ違いの打線で譜面の形が変わる。シュールが温度で化ける。
- **検証**: 特定タグだけの打線をデバッグ投入して譜面目視。

## P2-03 漫才プレイ演出の宿題2件
- **目的**: `00_OVERVIEW §10.1` の残り(1と2。3はP2-01で消化済み)。
- **触る**: `manzai-play.html`
- **手順**: ①能力/特殊能力カットインを**画面左側**表示に移設。②Perfect時に客の笑い声SE(WebAudio合成: ノイズ+ピッチ揺らぎの短音を重ねる。ファイル追加禁止)。
- **受け入れ**: 実機で目視/試聴。00_OVERVIEW §10.1のチェックを消す(md更新も同コミット)。

## P2-04 審査員システム(決勝)
- **目的**: 決勝の採点を「5人の顔が見える」ものに。
- **参照**: `SCORING §7`(全て設計済み。式・プール10名・検証例まである)
- **触る**: 新規 `js/data/judges.js`、`success-play.html`、`manzai-play.html`(detail返却のみ)
- **手順**:
  1. §7.4の10名をデータ化 `{id,name,persona,weights,bias}`。
  2. 決勝開始時(`applyAwardResult`で決勝ラウンドへ入るとき)に5名選出→`pm_honban.judges`。
  3. `manzai-play` は `pm_result.detail = {acc, perBeat:[判定配列], tags:{タグ別実績}, maxCombo, temp最終}` を返す(**採点はしない**)。
  4. 育成側で§7.3の式により5名の点数を算出→「審査員採点ボード」モーダル(1人ずつ点が開く演出・DESIGN.md準拠)→合計で勝敗。
- **受け入れ**: 決勝で5名の個別点が表示され、SCORING §7.5のサンプル値を入れると設計書の期待値±1に収まる。
- **検証**: サンプルネタのdetailを固定入力するテスト関数で照合。

## P2-05 決勝の別ネタ2本
- **目的**: `01_SUCCESS §6.2` の二段構え。
- **参照**: `01_SUCCESS §6.2`、`00_OVERVIEW 付録B`(決勝は別ネタ2本必須)
- **触る**: `neta-builder.html`、`success-play.html`、`manzai-play.html`
- **手順**:
  1. neta-builderに「ネタA/ネタB」タブ(`pm_lineup`→`{a:[...], b:[...]}`へ拡張。**旧形式(配列)からのmigrateを両読み側に実装**。ARCHITECTURE §3.1更新)。
  2. 決勝進出時にネタBが空なら警告(出番前に組ませる)。ファーストラウンド=A、最終決戦=B で `pm_honban.lineupKey` 指定。
  3. 最終決戦は上位3組演出(ライバル得点はP2-06)。
- **受け入れ**: 決勝で2本別ネタを演奏し、合算/最終決戦方式(§6.2の規定)で優勝判定される。
- **検証**: 3年目まで進めたデバッグセーブを用意して通し。

## P2-06 ライバル「平成レトロ」
- **目的**: 宿敵の可視化。
- **参照**: `04_STORY`(造形)、`01_SUCCESS §6.4`
- **前提(設計タスク)**: 2人の名前・口調が未確定(`00_OVERVIEW §10.2`)。**実装前にユーザーとブレストして `04_STORY` に確定内容を追記すること。勝手に確定しない。**
- **触る**: `success-play.html`、`js/data/scripts.js`
- **手順**: 確定後: M-1各ラウンド結果に平成レトロの通過状況を1行表示(内部的には年ごとの強さ曲線で得点生成)。決勝で得点対決演出。遭遇イベント(SCRIPTS S-03「余裕」等)をカレンダー/抽選に配置。
- **受け入れ**: 3年間でライバルの存在を最低3回感じる(遭遇/戦績/決勝)。

## P2-07 敗者復活(演出のみ)
- **参照**: `01_SUCCESS §6.5`、`00_OVERVIEW 付録B`(プレイヤー利用不可・演出のみ)
- **触る**: `success-play.html`
- **手順**: 準決勝敗退した年に限り、決勝週に「敗者復活の結果、○○(ライバル or モブ)が決勝へ」の短い紙芝居を挿入。プレイヤーは復活しない。
- **受け入れ**: 準決勝敗退年にのみ1回表示される。

---

# Phase 3. 物語とエンディング(β版)

> 依存順: P3-01(設計) → P3-02。P3-03 → P3-04。P3-05, P3-06 は後半並行。

## P3-01 月別カレンダー確定(設計チケット・実装なし)
- **目的**: 3年×48週の「いつ何が起きるか」の一表を確定する。
- **参照**: `01_SUCCESS §5.1`(カレンダー改訂メモ)、`04_STORY §9`(各年テーマ)
- **手順**: ユーザーとブレストし、S-01〜S-10・劇場出番・M-1日程・季節イベントを配置した表を `01_SUCCESS §5.1` に確定記載 → `js/data/calendar.js` の仕様として `ARCHITECTURE §4.4` を更新。**mdが確定するまで実装しない。**
- **受け入れ**: ユーザー承認済みのカレンダー表がmdにある。

## P3-02 メインシナリオS-01〜S-10実装
- **目的**: 物語の幹を全部入れる。
- **参照**: `SCRIPTS.md`(全脚本)、P3-01の確定カレンダー
- **触る**: `js/data/scripts.js`、新規 `js/data/calendar.js`、`success-play.html`
- **手順**:
  1. `calendar.js`: `{year, week, type:"script"|"theater"|"m1", id}` の一表。`isTheaterTurn()`/`m1RoundAt()`のハードコードを吸収(挙動が変わらないことを回帰確認)。
  2. S-01〜S-10を1本ずつsteps変換(台詞コピー厳守)。分岐(S-06のA/B等)は戦績フラグ(`S.m1Alive`等)を`cond`に。
  3. S-09はネタ作りコマンドのアンロック演出(`unlock`)、S-10は審査員体験(P2-04の機構を流用)。
  4. **1本変換するごとに`?script=`で目視**してから次へ(まとめて変換してまとめて壊れるのを防ぐ)。
- **受け入れ**: 3年通しで全脚本が正しい週・正しい分岐で再生される。
- **検証**: 週ジャンプのデバッグ関数(`goToTurn`活用)で全配置を巡回。

## P3-03 エンディング判定エンジン
- **目的**: 簡易5種を設計仕様の判定に置換。
- **参照**: `04_STORY §7.2`(上から評価・確定ロジック)、`§7.3`(引退回避ライン)、`00_OVERVIEW 付録B`(閾値80・突出能力重視)
- **触る**: 新規 `js/data/endings.js`、`success-play.html`(`openEnding`置換)
- **手順**:
  1. `endings.js`: `[{id, priority, cond:(S,result)=>bool, title, emoji, text, script?}]` を優先度順に評価。§7.4の確定分エンド+現行5種を移植(現行5種の文言は保持)。
  2. `openEnding` を差し替え: 判定→エンド演出(scriptがあれば紙芝居、無ければ現行モーダル)→`pm_meta.endings[id]=1`→リザルト画面(最終能力・図鑑進捗)。
  3. 引退回避ライン(準決勝進出+総合一定以上)を実装。
- **受け入れ**: 決勝勝利/決勝敗北/準決勝止まり/1回戦敗退の4パターンで異なる妥当なエンドが出る。
- **検証**: 能力・戦績を固定したデバッグ状態4種で判定表を照合。

## P3-04 エンディング図鑑UI
- **参照**: `04_STORY §7.1`
- **触る**: `index.html`(入口)、図鑑表示は軽量な新規HTMLか index 内モーダル(どちらでも可、遷移規約 `ARCHITECTURE §2` 準拠)
- **手順**: `pm_meta.endings` を読み、取得済み=タイトル+一言 / 未取得=「???」のグリッド。取得率表示。
- **受け入れ**: エンド取得後にタイトルから確認でき、周回・`clearSave`後も残る。

## P3-05 残イベント全量移植
- **参照**: `EVENTS.md` 残り全部(02,04,05,06,11,14,15,17,18,20,22,23,25,26,28,29,30,師匠の背中)
- **手順**: P1-05/06と同型で淡々と。23(路上ライブ)は本番往復プロトコルで漫才プレイへ接続。イベント抽選の重み・確率を`EVENT_RATES`で全体調整し、体感頻度(週送り15%目安)を維持。
- **受け入れ**: EVENTS.mdの「収録イベント一覧」と実装一覧が1:1対応(欠番除く)。対応表をEVENTS.md末尾に追記。

## P3-06 周回要素の仕上げ
- **参照**: `00_OVERVIEW §8`
- **手順**: 2周目以降: OP/既読スキップ、コンボ図鑑引き継ぎ(実装済み確認)、エンド図鑑からの「この条件を狙う」ヒント表示(💡任意)。引き継ぎ無しが原則(知識資産のみ)を確認しユーザーと合意。
- **受け入れ**: 2周目の開始が1周目より速くて快適。

---

# Phase 4. アート&演出統一

> P4-01〜03は`pixel-artist`系の反復作業+品質確認の分業が有効。**ART_RULES.mdの5人評議会チェックを必ず通す。**

## P4-01 yada表情差分5種(ドット絵)
- **参照**: `tools/PXART_PROCESS.md`(特にPhase 4: 目+眉の行だけ差替)、heroの前例=`_pxmap_hero_*.txt`
- **触る**: `_pxmap_yada_<expr>.txt` 新規4本(warai/odoroki/suberi/tsukkomi)
- **手順**: `_pxmap_yada.txt`(確定マスター)を複製→目+眉ブロックのみ編集→`tools/pxrender.ps1`で検証→heroのときと同様の確認HTML(5表情ならべ)をトンネルでユーザー承認→txtをコミット。**目+眉以外の行がバイト同一であることをスクリプトで機械照合。**
- **受け入れ**: ユーザー承認+バイト同一照合パス。

## P4-02 person1/person2 表情差分(各5種)
- P4-01と完全同型。person1は眼鏡(G)、person2はひげ(D/7)に注意(`tools/pxpalettes.ps1`)。

## P4-03 ゲームへのドット絵組込み
- **参照**: `ARCHITECTURE §6.1`(配置規約: `images/px/<char>_<expr>.png`)
- **触る**: `tools/pxrender.ps1`(出力先指定)、`success-play.html`、`manzai-play.html`、`index.html`
- **手順**:
  1. 全キャラ×全表情を `images/px/` へ実寸出力(拡大はCSS: `image-rendering:pixelated`)。
  2. `setFace`/`reactHero` の画像パスを差替(hero/partner→px版)。漫才プレイの立ち絵も同様。
  3. 旧絵(`images/hero*.png`等)は削除せず残す(gitに履歴があるが、参照ゼロになったことをGrepで確認してから次のPhaseで整理)。
- **受け入れ**: 全画面でキャラ絵の絵柄が統一。表情変化が旧実装と同じタイミングで動く。
- **検証**: トンネルでユーザー承認(絵の入替は必ず実機確認)。

## P4-04 DESIGN.md準拠の総点検
- **手順**: 4画面それぞれについてDESIGN.mdのDo's and Don'tsをチェックリスト化→違反を列挙してユーザーに見せる→承認された項目だけ修正(勝手に全直ししない。挙動リスクがあるため)。
- **受け入れ**: チェックリスト全消化 or 意図的な例外としてDESIGN.mdに追記。

## P4-05 画面遷移演出
- **手順**: 共通CSS(フェードアウト→`location.href`→フェードイン)を4画面に。`js/transition.js` として共有可。往復プロトコルのタイミングを壊さないこと。
- **受け入れ**: 全遷移が白フラッシュなしで繋がる。

## P4-06 音の仕上げ
- **参照**: `02_MANZAI_PLAY §4.9`
- **手順**: タイトルBGM(WebAudio合成・ループ)、文字送りSE統一、リザルトファンファーレ、エンディング曲(簡易)。全てWebAudio合成でファイル追加なし。iOSの解禁タイミングに注意。
- **受け入れ**: 無音の画面がなくなる(意図的な無音を除く)。

---

# Phase 5. バランス調整&QA

## P5-01 数値バランス確定
- **参照**: `research/gacha-tuner.html`、`research/neta-gacha-design.md`、`SCORING §8`
- **手順**: ①通しプレイ(普通にプレイ)で3年後の到達能力・決勝得点を記録 ②設計目標(`00_OVERVIEW 付録B`: 最終平均A2B2C1≈370等)と比較 ③乖離をgacha-tunerで調整→**mdの確定値を更新→実装値を合わせる**(この順)。M-1突破ボーダー(`M1_ROUNDS`の`thr`)も同時に確定。
- **受け入れ**: 「普通にプレイして2年目準決勝、上手くプレイして3年目優勝」の難度カーブ(目安)にユーザーが納得。

## P5-02 通しプレイQA
- **手順**: 4シナリオを実施し不具合を起票→修正: ①最速優勝狙い ②放置(コマンド適当) ③金欠/HP0の限界運用 ④セーブ破損(localStorage手動破壊)からの復帰。コンソールエラーゼロ徹底。
- **受け入れ**: 4シナリオ完走・致命バグゼロ。

## P5-03 実機検証(iPhone Safari)
- **手順**: トンネルで実機チェックリスト: タッチ判定遅延(リズムゲームの許容窓)、オーディオ解禁、100dvh(URLバー出没)、ホーム画面追加時の表示、スリープ復帰。問題は個別チケット化。
- **受け入れ**: ユーザーの実機で全画面プレイ可能。

## P5-04 リポジトリ衛生
- **手順**: `.gitattributes`(`* text=auto eol=lf`目安)導入とCRLF警告解消、参照ゼロの旧アセット(`images/hero*.png`等)の`_archive/`退避、README/各mdの現状同期。
- **受け入れ**: `git status`常時クリーン・警告なし。

---

# Phase 6. リリース(v1.0)

## P6-01 Service Worker(PWA完成)
- **参照**: `ARCHITECTURE §8`
- **手順**: `sw.js`新規(プリキャッシュ: HTML4+js/+images/px/+mp3+icons。キャッシュ名にバージョン文字列、activate時に旧キャッシュ削除)。`index.html`で登録。**キャッシュ戦略をARCHITECTURE §8へ追記。**
- **受け入れ**: 機内モードで全画面が動く(初回ロード後)。更新配信で新版に切り替わる。

## P6-02 配信
- **参照**: `00_OVERVIEW §10.3`(Public→GitHub Pages / Private維持→Netlify)
- **手順**: **どちらにするかユーザーに確認**(現リポジトリはPrivate想定)。Pages なら Settings→Pages を案内、Netlify ならドラッグ&ドロップ or CLI手順を提示。配信URLで P5-03 のチェックリストを再実施。
- **受け入れ**: 公開URLで初見の端末からプレイできる。

## P6-03 最終化
- **手順**: タイトル正式決定(ユーザー決定事項)、タイトル画面/manifest/アイコンへ反映、クレジット表記、README冒頭を「遊べるURL」付きに更新、`v1.0` タグを打つ。
- **受け入れ**: `git tag v1.0` とリリースURLが存在する。

---

## 付録A. チケット消化の記録欄

> 完了したら1行追記する(セッションを跨いだ引き継ぎメモ)。

| 日付 | チケット | 実施モデル | 一言メモ |
|------|----------|-----------|----------|
| 2026-07-03 | (Phase 0 完了) | Fable 5 | リポジトリ整理・ROADMAP/ARCHITECTURE/本書作成 |

## 付録B. 迷ったときの優先順位

1. **ユーザーの承認 > 設計md > 本書のチケット記述**(古くなっている可能性が最も高いのは本書)。
2. 仕様が読み取れない → 実装せずに質問する。仮実装するなら「※値は仮」コメント+md逆反映をセットに。
3. 動くものを壊さない。回帰確認(§0.4-3)を省略しない。
4. 台詞・脚本は**一字一句コピー**。AIの創作で置き換えない(創作が必要なら提案としてユーザーに出す)。
